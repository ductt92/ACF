import React from 'react';

import AdminLayOut from '@/layout/AdminLayOut';

import ConnectContainer from './container/connect';

const Connect = () => {
  return <ConnectContainer />;
};

Connect.Layout = AdminLayOut;

export default Connect;
